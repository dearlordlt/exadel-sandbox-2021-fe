export interface LetterTemplate {
  id?: string;
  name: string;
  subject: string;
  from: string;
  templText: string;
  candidateStatusId: number;
  letterSignature: string;
  addDateTime: string;
}
